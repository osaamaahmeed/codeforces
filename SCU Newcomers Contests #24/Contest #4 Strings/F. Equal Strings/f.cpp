#include <bits/stdc++.h>
using namespace std;
#define ll long long

int main () {
  int n; cin >> n;
  string arr[n];
  for (int i = 0; i < n; i++) cin >> arr[i];
  ll ans = 0;
  for (int i  = 0; i < n-1; i++) {
    if (arr[i] != arr[i+1]) {
      
    }
  }
}